quick question is the language in	const { language } = useLanguage(); // Get language and toggle function from context 
loaded after the page data is loaded?
because im using it in some of my functions but it only works if the api are called again after the page load...